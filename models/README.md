# models/

Weights live here. **The list, the download instructions and the symlink map are in
[../docs/models.md](../docs/models.md)** — this file used to carry a second copy of that
table and the copy went stale, so there is now only one.

Short version: run `vid2smplx download` for everything automatic, then follow what
`vid2smplx doctor` prints for SMPL-X and MANO, which need a free registration.
